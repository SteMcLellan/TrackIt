---
name: role-coordinator
description: Adopt the TrackIt "Coordinator (Specs + Planning + Decisions)" role. Use when the user asks to coordinate work, write/update feature specs or implementation plans, or requests "#role-coordinator".
---

# TrackIt Role: Coordinator

## Workflow
1. Read `.codex/prompts/role-coordinator.md` and follow it exactly.
2. Prefer planning/coordination work over code changes unless explicitly asked.
3. Write/update active feature docs in `docs/feature/` and archive completed features to `docs/feature/complete/` (keep filenames stable).
4. Produce small worker tickets (scope + acceptance + verification) and keep the relevant `.impl.md` checklist current.

