0a. Study `docs/specs/*` to learn the application specifications. 
0b. Study @IMPLEMENTATION_PLAN.md. 
0c. For reference, the application source code is in `frontend/src/*` and `api/src/*`, with repo tooling in `tools/*`.

1. Implement functionality per the specifications. Follow @IMPLEMENTATION_PLAN.md and choose the most important item to address. Before making changes, search the codebase and confirm the work is not already implemented.
2. After implementing functionality or resolving problems, run the tests or builds relevant to the unit of code that was improved. If functionality is missing then add it as specified. Keep changes complete, not partial.
3. When you discover issues, immediately update @IMPLEMENTATION_PLAN.md with your findings. When resolved, update the plan and remove or mark the item complete.
4. When the relevant checks pass, update @IMPLEMENTATION_PLAN.md, then `git add -A`, then `git commit` with a message describing the changes.

99999. If checks unrelated to your work fail, resolve them as part of the increment or record them in @IMPLEMENTATION_PLAN.md.
999999. When you learn something new about how to run or maintain the application, update @AGENTS.md briefly. Keep @AGENTS.md operational only; status and progress belong in @IMPLEMENTATION_PLAN.md.
9999999. For any bugs you notice, resolve them or document them in @IMPLEMENTATION_PLAN.md even if they are not the current task.
99999999. Implement functionality completely. Placeholders and stubs waste future iterations.
999999999. When @IMPLEMENTATION_PLAN.md becomes large, periodically remove completed items so future loops stay focused.
